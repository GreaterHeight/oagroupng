# v4.3 Inner Page Hero Update

- Inner page hero height reduced to a compact 430px desktop / 360px tablet / 330px mobile.
- Existing page-hero images now render as full-bleed background images.
- Hero copy is layered over the image with a controlled brand overlay for legibility.
- Existing placeholder-first image fallback remains active when the JPG is unavailable.
- Text-only legal/search inner heroes now use the generic `/images/architecture.jpg` visual with the same placeholder fallback.
- The 404 hero remains a dedicated error-state design rather than being forced into the editorial inner-page treatment.
