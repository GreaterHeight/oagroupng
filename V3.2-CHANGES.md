# OA Group Nigeria — v3.2 Changes

- Replaced the primary-navigation “Search” text link with an icon-only SVG search control positioned after the CTA.
- Preserved accessibility with `aria-label="Search"`, tooltip/title text, keyboard focus styling and a 44px target.
- Added an explicit generation prompt for `videos/oagroup-hero.mp4`.
- Added an explicit generation prompt for `images/hero-video-poster.jpg`.
- Updated `IMAGE-PROMPTS.md` with both hero asset prompts, technical targets and negative prompts.
- No external video or image generation service is invoked by the static website itself; the prompts are intended for the asset-production workflow.
