document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("[data-filter-group]").forEach(group=>{const scope=group.closest("section")||group.parentElement||document;const buttons=[...group.querySelectorAll("[data-filter]")];const items=[...scope.querySelectorAll("[data-filter-item]")];if(!buttons.length||!items.length)return;const applyFilter=filter=>{buttons.forEach(button=>button.setAttribute("aria-pressed",String(button.dataset.filter===filter)));items.forEach(item=>{const show=filter==="ALL"||item.dataset.filterItem===filter;item.classList.toggle("is-filter-hidden",!show);item.setAttribute("aria-hidden",String(!show))})};buttons.forEach(button=>button.addEventListener("click",()=>applyFilter(button.dataset.filter)));applyFilter("ALL")})});
