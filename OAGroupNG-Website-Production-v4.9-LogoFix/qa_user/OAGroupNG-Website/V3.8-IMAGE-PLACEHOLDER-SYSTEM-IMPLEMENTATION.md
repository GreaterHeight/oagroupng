# OA Group Nigeria — v3.8 Image Placeholder / Auto-Swap Implementation

Implemented the updated Image Placeholder / Auto-Swap System across the website.

## Results

- Audited `93` local `<img>` image slots.
- Failure handling present on `93/93`.
- Added `0` missing simple `onerror="this.remove()"` handlers.
- Converted/retained `0` rich placeholder containers using the documented
  `.is-empty` failure-state pattern.
- Placeholder CSS is hidden by default and revealed only when `.is-empty` exists.
- Restored `/our-companies/` to its canonical `/images/our-companies-portfolio.jpg` path.
- Did not introduce a custom image-swap registry or filename detection system.
- Video poster remains a separate video fallback mechanism.

## Important operational rule

Place each generated JPG at the exact filename already referenced by the HTML.
The browser then renders it automatically on the next page load.
