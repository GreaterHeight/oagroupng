document.addEventListener("DOMContentLoaded",()=>{const modal=document.querySelector("[data-lightbox-modal]");if(!modal)return;const media=modal.querySelector("[data-lightbox-image]"),caption=modal.querySelector("[data-lightbox-caption]"),close=modal.querySelector("[data-lightbox-close]"),prev=modal.querySelector("[data-lightbox-prev]"),next=modal.querySelector("[data-lightbox-next]");const items=[...document.querySelectorAll("[data-lightbox]")];if(!media||!caption||!close||!prev||!next||!items.length)return;let index=0,lastTrigger=null;const focusables=()=>[close,prev,next].filter(el=>el&&!el.disabled);const render=()=>{const el=items[index];media.src=el.dataset.src||"";media.alt=el.dataset.alt||"";caption.textContent=el.dataset.caption||""};const open=i=>{lastTrigger=items[i];index=i;render();modal.classList.add("is-open");modal.setAttribute("aria-hidden","false");document.body.style.overflow="hidden";close.focus()};const shut=()=>{modal.classList.remove("is-open");modal.setAttribute("aria-hidden","true");document.body.style.overflow="";if(lastTrigger)lastTrigger.focus()};const move=delta=>{index=(index+delta+items.length)%items.length;render()};items.forEach((el,i)=>el.addEventListener("click",()=>open(i)));close.addEventListener("click",shut);prev.addEventListener("click",()=>move(-1));next.addEventListener("click",()=>move(1));modal.addEventListener("click",e=>{if(e.target===modal)shut()});document.addEventListener("keydown",e=>{if(!modal.classList.contains("is-open"))return;if(e.key==="Escape"){e.preventDefault();shut()}else if(e.key==="ArrowLeft"){e.preventDefault();move(-1)}else if(e.key==="ArrowRight"){e.preventDefault();move(1)}else if(e.key==="Tab"){const f=focusables();if(!f.length)return;const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});media.addEventListener("load",()=>{media.closest(".modal__media")?.classList.remove("is-empty")});
media.addEventListener("error",()=>{
  media.closest(".modal__media")?.classList.add("is-empty");
  media.removeAttribute("src");
  caption.textContent=(caption.textContent?caption.textContent+" — ":"")+"Image unavailable.";
});
