document.addEventListener('DOMContentLoaded',()=>{const modal=document.querySelector('[data-lightbox-modal]');if(!modal)return;const media=modal.querySelector('[data-lightbox-image]'),caption=modal.querySelector('[data-lightbox-caption]'),close=modal.querySelector('[data-lightbox-close]'),prev=modal.querySelector('[data-lightbox-prev]'),next=modal.querySelector('[data-lightbox-next]');const items=[...document.querySelectorAll('[data-lightbox]')];let index=0;const render=()=>{const el=items[index];media.src=el.dataset.src;media.alt=el.dataset.alt||'';caption.textContent=el.dataset.caption||''};const open=i=>{index=i;render();modal.classList.add('is-open');modal.setAttribute('aria-hidden','false');close.focus()};const shut=()=>{modal.classList.remove('is-open');modal.setAttribute('aria-hidden','true')};items.forEach((el,i)=>el.addEventListener('click',()=>open(i)));close.addEventListener('click',shut);prev.addEventListener('click',()=>{index=(index-1+items.length)%items.length;render()});next.addEventListener('click',()=>{index=(index+1)%items.length;render()});modal.addEventListener('click',e=>{if(e.target===modal)shut()});document.addEventListener('keydown',e=>{if(!modal.classList.contains('is-open'))return;if(e.key==='Escape')shut();if(e.key==='ArrowLeft')prev.click();if(e.key==='ArrowRight')next.click()})});
