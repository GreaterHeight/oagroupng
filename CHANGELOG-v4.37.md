# v4.37 — Production Form Delivery

- Replaced Contact `mailto:` workflow with FormSubmit AJAX delivery.
- Replaced Careers `mailto:` workflow with FormSubmit AJAX delivery.
- Preserved Careers CV upload and increased client-side validation ceiling to 10 MB.
- Replaced newsletter `mailto:` workflow with FormSubmit AJAX delivery.
- Added FormSubmit CSP permissions.
- Added in-page loading, success and error feedback.
- Added native POST fallback.
- Added one-time activation/setup documentation.
