# OA Group Nigeria — v4.36 Products Hero Placeholder QA

## Implemented
- `/products/` page hero now follows the same placeholder-first architecture used by `/about/`.
- Hero image slot: `/images/products-hero.jpg`.
- The JPG is intentionally not supplied; the page renders the approved image placeholder until the final asset is added.
- Added a dedicated generation prompt to `IMAGE-PROMPTS.md`, including the required 16:9 / minimum 3840×2160 JPG specification, text-safe left third, OA Group visual direction and negative prompt.
- Existing global placeholder/error handling remains responsible for failed or unavailable image assets.

## Verification
- Confirmed `/products/index.html` contains the hero image slot and visible placeholder markup.
- Confirmed `/images/products-hero.jpg` is not present, so the placeholder-first state is intentional.
- No generated hero image has been added to the production build.
- Browser visual QA was not performed because the available Chromium environment has previously returned `ERR_BLOCKED_BY_ADMINISTRATOR`; no visual pass is being claimed.
